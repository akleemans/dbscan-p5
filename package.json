{
  "name": "dbscan-p5",
  "version": "0.0.1",
  "description": "Interactive DBSCAN demo in P5",
  "scripts": {
    "start": "run-p start-compile start-run",
    "start-compile": "tsc --watch",
    "start-run": "browser-sync start --server -w --startPath /docs"
  },
  "devDependencies": {
    "@types/p5": "1.7.6",
    "browser-sync": "3.0.2",
    "npm-run-all": "4.1.5",
    "typescript": "5.4.3"
  }
}
